// Firebase Configuration for Anonymous Auth
// Initialize Firebase in your HTML if needed for future interactive features

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "dream-center-mhs.firebaseapp.com",
  projectId: "dream-center-mhs",
  storageBucket: "dream-center-mhs.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};

// For this static site, Firebase hosting serves the content directly
// Anonymous auth is available if needed for future interactive features
// Initialize with: firebase.initializeApp(firebaseConfig);
// Sign in anonymously: firebase.auth().signInAnonymously();
